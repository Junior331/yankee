import React from "react";
import { Text } from "react-native";
import { Layout } from "@/components/organism";

export const Search = () => {
  return (
    <Layout>
      <Text>Search</Text>
    </Layout>
  );
};
